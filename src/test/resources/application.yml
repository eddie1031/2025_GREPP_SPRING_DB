spring:

  datasource:
    url: jdbc:h2:mem:test;MODE=MYSQL;
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create

mybatis:
  type-aliases-package: io.eddie.dao.global.entity
  configuration:
    map-underscore-to-camel-case: true
  mapper-locations: classpath:mappers/*.xml


